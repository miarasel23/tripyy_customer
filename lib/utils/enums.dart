enum OtpStatus { initial, loading, success, failure }

enum OtpReceiveStatus { initial, loading, success, failure }

enum SplashStatus { initial, loading, success, failure }

enum EditProfilePictureStatus { initial, loading, success, failure }

enum EditProfileUpdateStatus { initial, loading, success, failure }

enum ChooseCarBottomSheetStatus { initial, loading, success, failure }
